{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "secretsmanager:GetSecretValue"
      ],
      "Resource": "arn:aws:lambda:{{it.Region}}:{{it.AccountId}}:secret:{{it.SecretsStoreId}}"
    }
  ]
}