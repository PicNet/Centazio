{
  "Defaults": {
    "ReadFunctionPollExpression": "*/1 * * * ? *",
    "PromoteFunctionPollExpression": "*/3 * * * ? *",
    "WriteFunctionPollExpression": "*/3 * * * ? *",
    "OtherFunctionPollExpression": "*/3 * * * ? *"
  }
}