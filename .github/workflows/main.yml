name: .NET CI

on:
  push:
    branches:
      - master
      - devops
      - dev
    paths-ignore:
      - '**/*.md'
      - '**/*.user'
      - '.git/**'

jobs:
  build:
    runs-on: self-hosted

    env:
      dotnetver: '9.x'
      buildcfg: 'Debug'

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Check Docker version
      run: sudo docker version

    - name: dotnet info
      run: sudo dotnet --info

    - name: dotnet restore
      run: sudo dotnet restore --verbosity normal

    - name: dotnet build
      run: sudo dotnet build --configuration ${{ env.buildcfg }} --verbosity normal --no-restore

    - name: dotnet test
      run: sudo dotnet test --no-restore --no-build
