name: .NET CI

on:
  push:    
    paths:
      - '**.cs'
      - '**.json'

jobs:
  build:
    runs-on: self-hosted
    timeout-minutes: 30

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x
        dotnet-quality: preview

    - name: Check Docker version
      run: docker version

    - name: Upgrade Azure CLI
      run: az upgrade --yes

    - name: dotnet info
      run: dotnet --info

    # note this `dotnet restore` is required even though there should 
    #   be [implicit restore](https://learn.microsoft.com/en-us/dotnet/core/tools/dotnet-build#implicit-restore)
    - name: dotnet restore
      run: dotnet restore Centazio.Core.Tests --verbosity normal
      
    - name: dotnet build
      run: dotnet build Centazio.Core.Tests --configuration Debug --verbosity normal --no-restore

    - name: dotnet test
      run: dotnet test Centazio.Core.Tests --no-restore --no-build --filter Test_notification_works
